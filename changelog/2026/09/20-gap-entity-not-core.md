# `gap` entity is not ASC core

| Field | Value |
|-------|--------|
| **Date** | 2026-09-20 |
| **Status** | done (rule). No entity YAML. No README.md edit. |
| **Scope** | [asc-lightweight.mdc](../../../.cursor/rules/asc-lightweight.mdc) — knowledge/task `gap` stays out of this tree. |

`$` in this file is the ASC docs placeholder (`$subject` / `$action`), not a shell variable.

---

Locked: do not add `gap.entity.yml` / `gap.able.yml` (or a core `gap` type) in this ASC **core** repo.

A knowledge gap and a task gap may later share a contract in a **project instance**. The named product is [Projet Complexe](../../../README.md) (README § Example project). Instance-local notes belong in that stack (`projet-complexe-asc`), not here.

Core already has the facing words: Workflow **Doubts** / **Ideas**, `data/entities/queue`, changelog, README mismatches. Entity work in this repo stays [10-begin-entity-system-with-remote-instances.md](./10-begin-entity-system-with-remote-instances.md) (`host` / `remote_instance` load).
