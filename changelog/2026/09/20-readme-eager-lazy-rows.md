# README proposal: eager / lazy example rows

| Field | Value |
|-------|--------|
| **Date** | 2026-09-20 |
| **Status** | **applied** (2026-09-20). Recap table + Exceptions sentence are in root [`README.md`](../../../README.md). Later apply pass left Recap at five rows (no `fs.opt-inc.sh` / `db.opt-inc.sh` dump). |
| **Target** | [`README.md`](../../../README.md) — **Always (= eager) VS conditionally (= lazy) sourced includes** → **Recap** (and stray lines under it). |

`$` in this file is the ASC docs placeholder (`$subject` / `$action` / `$extension`), not a shell variable.

---

## Why

The Recap table only showed two eager name-matches, then `TODO [wip]`, then leftover chat pointing.

## Not this edit

Did not paste the working case table. Did not add `fs.opt-inc.sh` / `db/db/db.opt-inc.sh` rows (one example per idea). Did not rewrite the two lazy bullets.
