# README: genericity scale vs lookup axis

| Field | Value |
|-------|--------|
| **Date** | 2026-09-20 |
| **Status** | **applied** (2026-09-20). Root [`README.md`](../../../README.md). |
| **Target** | **Genericity (scale)** plus lookup lists, cache, tests, Getting started. |

`$` in this file is the ASC docs placeholder (`$subject` / `$action` / `$extension`), not a shell variable.

---

## Why

Scale words (Primordial … Specifics) were reused for lookup depth, discovery cache, kernel includes, YAML `override:`, and instance files.

## Exact edit

One clause per idea, already in README: two axes (kind vs lookup); kernel = five includes; runtime discovery ≠ YAML Primitives; Overrides = `scripts/asc/override` swap; Specifics = `scripts/asc/extend`; make names prefix across namespaces.

## Not this edit

Did not dump a 20-row synonym table. Did not rename `f_asc_primitives_cache_ensure`. Did not rewrite the entity `override:` examples. DSL heading kept.
