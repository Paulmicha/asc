# Garage clause in the always-applied rule

| Field | Value |
|-------|--------|
| **Date** | 2026-09-20 |
| **Status** | done (rule). No runtime code. |
| **Scope** | [asc-lightweight.mdc](../../../.cursor/rules/asc-lightweight.mdc) — one file, no new `.mdc`. |

`$` in this file is the ASC docs placeholder (`$subject` / `$action`), not a shell variable.

---

Set in the mother always-applied rule: garage (explore, break frozen checklists), wild **the minimal way**, trust via **literal ASC tests** (unrun = unverified), **constant self-improvement** at every step (smallest rule/changelog line; instance-only facts stay in that instance).

Does not add the opt-inc mirror path (still a plan apply). Does not launch the meadows loop.
