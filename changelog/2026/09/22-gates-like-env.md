# Gates file follows env.yml lookup

> **2026-09-25 concertation.** This file is done. [25-concert-order.md](./25-concert-order.md) does not reopen it.

| Field | Value |
|-------|--------|
| **Date** | 2026-09-22 |
| **Status** | implemented |
| **Scope** | Docroot gates discovery for the mother checkout. |
| **Not this change** | A synonym table that makes hook lookup treat `mother` as `core`. Wiring `mother` into `f_hook_variant_values_add`. |

`$` in this file is the ASC docs placeholder (`$subject` / `$action`), not a shell variable.

---

## What

`SPECIMEN.gates.yml` is the anonymized copy model, same role as `SPECIMEN.env.yml`. The live registry for this checkout is `gates.core.yml`. `hook_ms 'dry-run' -s 'doc' -a 'gates' -c 'yml' -v 'STACK_VERSION HOST_TYPE INSTANCE_TYPE' -r` returns that file when `INSTANCE_TYPE` is `core`.

This checkout sets that value in gitignored `.env-local.yml` (`instance_type: core`). `.env` and `data/asc/globals.sh` are regenerated by instance reinit so the readonly global matches.

`core` is the instance-type token. `mother` is the prose name for this same checkout. Hook variants use the literal `core`. A synonym that also matches `gates.mother.yml` is not wired.

## Check

`asc/test/core/next_steps.test.sh` — `test_gates_yml_docroot_rung` expects `gates.core.yml` when `INSTANCE_TYPE` is `core`, and expects the specimen to stay anonymized.
