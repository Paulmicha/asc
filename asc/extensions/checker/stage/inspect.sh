# TODO this entry point reads the staged blob of each path in the index
# (`git show :path`), not the unified diff and not the worktree file.
# It compares those contents to the anti-pattern catalog.
