# TODO this entry point fetches the anti-patterns that apply to staged contents.
# When those contents belong to an ASC project instance repo, the list is the
# "asc" group, in `data/entities/anti-pattern/asc`.
