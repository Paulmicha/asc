# TODO this entry point fetches a list of anti-patterns that are susceptible to apply to any given "git diff" context.
# If the git diff is ran on a "dev stack" repo work tree (= an ASC project instance repo), then the anti-patterns list to load would be "asc core" anti-patterns list, in `data/entities/anti-pattern/asc`.
